{
  "name": "cor-del-mon-tests",
  "version": "1.0.0",
  "private": true,
  "description": "Smoke and e2e tests for Cor del Món. Tooling only, not part of the deployed site.",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:smoke": "playwright test smoke.spec.js",
    "test:e2e": "playwright test e2e.spec.js"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0",
    "ajv": "^8.20.0",
    "ajv-formats": "^3.0.1"
  }
}
